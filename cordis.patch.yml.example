# dsh-openviking 插件接线片段（clone 快速路径）
# 复制本文件的 insert 块到 dsh profile 的 patch 层：
#   /root/.dsh/profiles/web/cordis.patch.yml    （Web profile）
#   或 ~/.dsh/cordis.patch.yml                  （默认 profile）
# 不要动 cordis.yml 本体。
#
# 两种接线模式（二选一，id 相同勿重复）：
#   A. clone 快速路径（本文件）：name 用绝对路径，config 直接填 url/key，
#      零补丁零构建，本机 localhost 即可用 —— 见 README「快速开始」
#   B. 包安装路径：pnpm add github:abc12524/dsh-openviking 后，仓库内
#      cordis.patch.yml（bundle manifest）由 profile composer 自动接线，
#      无需手写本片段 —— 见 README「方式 B」
- insert:
    - id: openviking
      # 路径型 entry：tsx 直接加载 src，host 半段无需构建
      name: '/path/to/dsh-openviking/src/index.ts'
      config:
        # 直接写配置 = 不打开设置页也能用（推荐）
        url: http://<openviking-host>:1933/mcp
        key: '<bearer-token>'
        minScore: 0.4
        # maxResults: 3
